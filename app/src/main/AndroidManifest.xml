<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flyyuan.contract_firm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".GApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.AboutPage" />
        <activity android:name=".GetCompanyActivity" />
        <activity
            android:name=".activity.HRtribuneActivity"
            android:label="@string/title_activity_hrtribune"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.HRFoundActivity"
            android:label="@string/title_activity_hrfound"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.HRClassActivity"
            android:label="@string/title_activity_hrclass"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.ContractModelActivity" />
        <activity android:name=".activity.ModelPDFActivity" />
        <activity android:name=".activity.NewContractActivity" />
        <activity android:name=".activity.ContractInformationActivity" />
        <activity android:name=".activity.PendingContractActivity" />
        <activity android:name=".activity.SealActivity" />
        <activity android:name=".activity.AlreadyLaunchedActivity" />
        <activity android:name=".activity.AlreadySignActivity" />
        <activity android:name=".activity.VouchActivity"></activity>
    </application>

</manifest>